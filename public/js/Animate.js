
window.onload = init
function init() {

    var containers = document.getElementsByClassName("cardContainer");
    var surfaces = document.getElementById("card");

    if(container.length) {
    }
   
    console.log(containers);
    console.log(surfaces);
}